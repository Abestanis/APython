<resources>

    <!-- Base application theme. -->
    <style name="PyHostAppTheme" parent="android:Theme.Light.NoTitleBar">
        <!--<item name="android:windowBackground">@color/background_color</item>-->
    </style>

    <style name="MainMenuTheme" parent="android:Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/background_color</item>
    </style>

    <style name="TerminalTheme" parent="android:Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
    
    <style name="AppDialogTheme" parent="android:Theme.DeviceDefault.Light.Dialog">
        <!--<item name="android:windowBackground">@android:color/transparent</item>-->
    </style>

</resources>
